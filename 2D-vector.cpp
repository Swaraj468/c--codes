#include<bits/stdc++.h>
using namespace std;
int main(void){
    // 2D array vector
    vector<vector<int>>arr(5, vector<int>(4, 0));
    // 2D array created with 5 rows with 4 columns, with 0 initial values;
    // for rows count
    int total_rows = arr.size();
    // ;for columns count
    int total_columns = arr[0].size();
    cout<<total_rows<<" "<< total_columns<<" "<<endl;
    
}